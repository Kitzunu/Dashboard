{
  "name": "azerothcore-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start:server-agent\" \"npm run start:backend\" \"npm run start:frontend\"",
    "start:server-agent": "cd backend && node serverAgent.js",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
